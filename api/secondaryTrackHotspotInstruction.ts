/** Appended to user context for secondary / more-stops JSON generation. */
export const TRACK_HOTSPOTS_USER_CLAUSE = `Each JSON object must include "hotspots": an array (0–10 items). Each hotspot uses: id (string), kind one of wikipedia|places|inferred|persona|unknown, title (short), body (1–3 sentences), optional url (https only), excerpt (exact contiguous substring copied from this object's "script" field; excerpt must still appear verbatim after cleaning: remove lines starting with #, drop ALL-CAPS section heading lines, remove [...] and (...) asides and asterisks, trim lines). Use kind "persona" for deliberate invention.`
